# Keep Profile management and coding workflows separate

MetaPi's current approved workflow scope is Profile management: discovery, import, export, update reminders, directory binding, status, and diagnostics. Coding workflows remain user-owned and `PLANNED`; MetaPi must not invent plan/review/implementation semantics, install related plugins, or enable them implicitly. Any later coding workflow is a separate explicitly defined change.
