---
status: superseded by ADR-0025
---

# Preserve Pi environment behavior for the default Profile

MetaPi originally assigned complete host-environment inheritance to `default`. This decision was superseded when original Pi compatibility moved to the reserved `pi` Profile and MetaPi `default` became the isolated Starter Profile.
