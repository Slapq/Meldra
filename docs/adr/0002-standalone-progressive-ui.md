---
status: superseded by ADR-0003
---

# Build a standalone application with a progressive UI

Meldra was next conceived as a standalone application that consumed Pi through its public SDK and reused `InteractiveMode`. This decision was superseded when Meldra was clarified as Pi itself: a downstream source distribution that preserves the complete upstream implementation and applies Meldra features as a separate patch layer.
