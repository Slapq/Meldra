# Withdraw Profile and session Theme scopes

> Superseded by ADR 0035.

The previously proposed Profile Theme Default, Session Theme Override, and advanced three-scope Theme menu will not be implemented. Theme remains Pi's global user setting across ordinary MetaPi Profiles. Pi's existing project settings override and Theme resource discovery behavior remain unchanged, while the reserved `pi` compatibility Profile keeps original Pi behavior and state.
