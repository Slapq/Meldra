# Keep coding workflows user-owned

The Starter Profile includes Profile management workflows. Meldra will not independently design or implement coding workflows such as plan, review, or implementation modes, and will not install or enable plugins for them. Those workflows remain `PLANNED` until the user provides their exact product and implementation definition; when later authorized, each named workflow is a separate scope with its own tests and rollback boundary.
